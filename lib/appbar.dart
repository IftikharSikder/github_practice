import 'package:flutter/material.dart';

appBarStyle() {
  return AppBar(
    backgroundColor: Colors.teal,
    centerTitle: true,
    title: const Text("Headspace",style: TextStyle(fontWeight: FontWeight.bold),),
  );
}
